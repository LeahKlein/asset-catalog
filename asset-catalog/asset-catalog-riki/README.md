# Assets catalog

To save all assets in a distributed system.

The customer writes in the cli the property he wants to add to the catalog, each property can only be uploaded once.

To upload an asset navigate the cli into the directory and write

```Python
python <filename>.py <asset>
```
