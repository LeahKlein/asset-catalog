# Asset Catalog

The asset-catalog is the CLI tool that run on a remote machine.

The client can watch the names of the files that in the server from the cache that contains the names files or upload files to the remote server but only if it doesn't exist in the server, the cache check it before upload to the server!!
The client can't change the watch directory at any point!
